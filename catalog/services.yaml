# CI seed targets for Rebuild Platform — repos that build/push images.
# Not the full Argo inventory (see cluster-state); only services with release workflows.
# Day-to-day prod promotion still goes through release.yaml's production environment gate.
# Rebuild waits for deploy-dev only; promote-prod can be approved later on the same tag.
services:
  - repo: QRify-platform/qrify-web
    workflows:
      - release.yaml
  - repo: QRify-platform/qrify-web-api
    workflows:
      - release.yaml
  - repo: QRify-platform/qrify-portal
    workflows:
      - release.yaml

argo_sync:
  repo: QRify-platform/cluster-state
  workflow: argo-sync.yaml
